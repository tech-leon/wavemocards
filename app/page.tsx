"use client";
// import { useTranslation } from "react-i18next";
import Hero from "@/components/hero";

export default function Home() {
  // const { t } = useTranslati/on();
  return (
    <main className="flex h-full flex-col items-center justify-center">
        <Hero />
    </main>
  );
}
